# Replit Deployment Configuration for React Todo App

[deployment]
run = ["node", "http-server.js"]
deploymentTarget = "autoscale"
build = ["npm", "run", "build"]

# Health check configuration
[deployment.health]
path = "/health"
port = 5000

# Environment configuration for production
[deployment.env]
NODE_ENV = "production"
PORT = "5000"

# Build optimization
[deployment.build]
ignoredPaths = [
  "node_modules",
  ".git",
  "src",
  "public",
  "*.md",
  "jest.config.ts",
  "jest.setup.ts",
  "babel.config.js",
  "postcss.config.js",
  "tailwind.config.js",
  "tsconfig.json",
  "tsconfig.node.json"
]